{
    "name": "certificates-generator",
    "private": true,
    "description": "Headless Chromium (Puppeteer) used by spatie/browsershot to render certificate PDFs.",
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "puppeteer": "^25.11.0"
    },
    "scripts": {
        "chrome:install": "puppeteer browsers install chrome"
    }
}
